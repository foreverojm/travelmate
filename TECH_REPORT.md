# TravelMate — 기술 리포트

> 여행자 통합 앱 (환율·맛집/명소·긴급 SOS를 한 앱으로)
> 콘셉트: **"이 앱 하나 = 앱 5~6개"** — 환율계산기·지도·번역·긴급연락·맛집앱을 통합
> 최초 작성: 2026-07-23 · 상태: **Phase 1 MVP 개발 중**

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 앱 이름 | TravelMate (가칭) |
| 프로젝트 경로 | `D:\TravelMate` |
| 대상 국가(1차) | 🇻🇳 베트남 · 🇯🇵 일본 · 🇹🇼 대만 · 🇹🇭 태국 |
| 개발 형태 | 1인 개발, 크로스플랫폼 |
| 출시 전제 | **Google Play 우선**, 추후 App Store 검토 |
| 수익화 방향 | 무료 출시 → (A) 프리미엄 전환 / (B) 무료+광고 (미확정) |

## 2. 기술 스택

- **Flutter 3.27.4 / Dart 3.6.2** (설치 경로 `C:\flutter`)
- 상태관리: `provider ^6.1.2`
- 네트워크: `http ^1.2.2` (환율 API)
- 로컬 저장/캐싱: `shared_preferences ^2.3.2`
- 포맷팅: `intl ^0.19.0`
- 외부앱 연동: `url_launcher ^6.3.1` (전화·지도)
- applicationId: `com.travelmate.travel_mate`

## 3. 아키텍처 / 폴더 구조

```
lib/
├─ main.dart                     # 앱 진입점 (Provider 주입)
├─ core/                         # 전역 공통
│  ├─ models.dart                # Country/Currency/Embassy/PhraseCard 등 모델
│  ├─ country_data.dart          # ★ 4개국 통화·긴급번호·대사관·회화·팁 데이터
│  ├─ theme.dart                 # 색/타이포 (절제된 딥톤 + 포인트 앰버)
│  ├─ format.dart                # 숫자/통화/상대시간 포맷터
│  └─ launcher.dart              # 전화걸기/지도열기 헬퍼
├─ home/
│  └─ home_shell.dart            # 하단 탭 4개 (IndexedStack)
└─ features/
   ├─ currency/                  # ① 환율 계산기 (MVP 핵심)
   │  ├─ rate_service.dart       # 온라인 갱신 + 오프라인 캐싱
   │  ├─ currency_provider.dart  # 상태·변환 로직
   │  ├─ currency_screen.dart    # 화면
   │  ├─ currency_picker.dart    # 통화 선택 시트
   │  └─ keypad.dart             # 전용 숫자패드
   ├─ places/                    # ② 맛집·명소 (관광객 vs 현지인)
   │  ├─ place_data.dart         # 시드(샘플) 데이터
   │  └─ places_screen.dart
   ├─ sos/                       # ③ 긴급 SOS
   │  └─ sos_screen.dart
   └─ more/                      # 더보기 (수익화 진입점·정책)
      └─ more_screen.dart
```

**설계 원칙**
- **오프라인 우선**: 환율은 캐시/시드로 인터넷 없이도 동작, 긴급/회화 데이터는 전부 정적.
- **데이터 중앙화**: `country_data.dart` 한 곳에서 모든 기능이 국가 데이터를 공유.
- **기능 격리**: `features/*` 단위로 폴더 분리 → 국가·기능 확장 시 영향 최소화.

## 4. 구현 완료 기능 (Phase 1 MVP)

### ① 환율 계산기 ✅
- **실시간 환율**: 앱 실행 시 open.er-api.com에서 최신 환율 조회('실시간·방금 갱신' 표시), 오프라인 시 캐시 폴백
- 통화: KRW · **USD**(이중가격 비교용) · VND · JPY · TWD · THB
- **동시 환산 중심 UI**: 입력 한 줄 + 전 통화 동시 표시(달러·동·원 한눈 비교). 통화 줄 탭 = 입력 통화 전환(스왑 대체). from→to 카드 제거로 한 화면에 모두 표시
- 전용 숫자패드(시스템 키보드 미사용, 햅틱 피드백, **C=전체삭제 버튼**, 백스페이스 길게=전체삭제)
- 즉시 양방향 변환 + 원터치 스왑
- KRW ↔ VND/JPY/TWD/THB 5통화
- **오프라인 우선**: 앱 시작 시 캐시/시드로 즉시 표시 → 온라인이면 백그라운드 실시간 갱신
- 실시간/오프라인 상태 + "N분 전" 갱신시각 표시, 탭하면 수동 새로고침
- 권종 치트시트(자주 쓰는 지폐 원터치 입력) — "이 지폐 얼마?"를 즉시
- 통화·금액 선택 shared_preferences로 기억
- 환율 API: `open.er-api.com` (무료·키 불필요·전 통화 지원)

### ② 맛집·명소 ✅ (큐레이션 확대)
- **관광객 vs 현지인** 구분 필터 (차별화 핵심)
- **국가 → 도시 2단 필터**: 나라별 '한국인이 자주 가는 도시' 목록(VN 하노이·호치민·다낭·호이안·나트랑·푸꾸옥 / JP 도쿄·오사카·교토·후쿠오카·삿포로·오키나와 / TW 타이베이·타이중·가오슝·화롄 / TH 방콕·치앙마이·푸켓·파타야). 나라 변경 시 도시 필터 초기화
- 맛집/명소 필터, 가격대 표시. **지도**: 좌표 있으면 정확 위치, 없으면 이름+도시로 지도 검색(모든 장소에 지도 버튼)
- **장소 총 490곳+**: 직접 큐레이션 80곳(현지인/관광객 태깅) + **OpenStreetMap 수집 416곳**
- **OSM 무료 수집 파이프라인**: `tools/fetch_osm.py`가 Overpass API로 20개 도시의 명소·음식점을 영문명 기준 수집(음식점은 품질 태그 필터) → `assets/data/osm_places.json` 생성
- **원격 로딩 + 오프라인 캐싱**(핵심): OSM 데이터를 앱에 하드코딩하지 않고 런타임에 JSON으로 로드. 앱 내장 asset(오프라인 기본) → 원격 URL(설정 시) 받아 캐싱. **→ 데이터 갱신에 앱 업데이트 불필요**
- **당겨서 새로고침 + 최근 업데이트 표시**: 맛집·명소 탭에서 리스트를 당기거나 새로고침 아이콘 탭 → 원격 최신 데이터 즉시 반영. 상단에 'N일 전 업데이트' 표시(JSON의 generatedAt 기준)
  - `places_repository.dart` / `places_provider.dart`. 원격 URL은 빌드 옵션 `--dart-define=PLACES_URL=<json주소>`
- **재수집 자동화**: `.github/workflows/refresh-places.yml`(주간 cron) — 스크립트 실행→JSON 커밋. 호스팅한 JSON만 갱신되면 사용자 앱은 자동 최신화
- OSM 수집분은 현지인/관광객 미분류 → 앱에서 **"지도 정보"** 태그로 구분. 출처 © OpenStreetMap(ODbL) 더보기 화면 명기
- 향후: Wikivoyage 보강, 사용자 '현지인 추천' 제보 크라우드소싱

### 이전/셋업
- `SETUP.md`: 다른 PC로 폴더째 복사해 이어서 개발하는 가이드(준비물·제외 폴더·빌드 명령). `tools/setup.ps1` 셋업 도우미.

### ③ 긴급 SOS ✅
- 나라별 현지 긴급번호 전화 (경찰/구급/소방/관광경찰) — 탭 시 다이얼러에 번호 입력된 채 열림(권한 불필요, Play 안전)
- **긴급 사이렌 버튼**: 합성 사이렌음(직접 생성한 `assets/audio/siren.wav`)을 반복 재생해 주변에 알림. 토글식(오작동 방지), 활성 시 펄스 표시
- 외교부 영사콜센터(24h), 한국 대사관/대표부: 대표번호·긴급번호·지도 길찾기
- **현지어 2종**: 긴급용(크게 띄워 상대에게 보여주기) + **일상 회화**(인사·감사·주문·계산·화장실·흥정·맛있어요) — 탭 시 전체화면
- **전원·콘센트**: 실제 콘센트 모양 도형(`core/plug.dart`, A/C형 등) + 전압·주파수 + 한국 플러그 호환 안내
- **카테고리별 여행 치트시트**: 통신·인터넷 / 교통·이동 / 결제·팁 / 물·음식 / 문화·예절 / 치안·사기 주의 (나라별 실전 정보)
- 현금·환전 팁, 전부 오프라인 동작, 면책 문구 포함

### 하단 탭 셸 ✅
- 환율 · 맛집·명소 · 긴급 · 더보기 (IndexedStack으로 상태 유지)

### UI 다듬기 ✅
- **국기 위젯 자체 렌더**(`core/flag.dart`): Android/웹 시스템 폰트가 국기 이모지(🇰🇷)를 렌더하지 못해 "KR"로 깨지던 문제를 CustomPaint로 직접 그려 해결(KR·VN·JP·TW·TH). 에셋/폰트 불필요.
- **커스텀 Pill 위젯**(`core/pills.dart`): Material Chip이 가로 스크롤에서 한글 라벨을 잘라먹던 문제를 대체 → 국가/필터 라벨 항상 온전히 표시.

## 5. 검증 현황

| 항목 | 결과 |
|------|------|
| `flutter analyze` | ✅ No issues found |
| `flutter test` (스모크) | ✅ All tests passed |
| Release APK 빌드 | ✅ 성공 (20.4MB, `build\app\outputs\flutter-apk\app-release.apk`) |
| 웹(Chrome) 구동 검증 | ✅ 4개 탭 전체 동작 확인 (환율 실시간 API 연결, 스왑/권종칩/현지어 전체화면/탭전환) |
| Android 실기기/에뮬레이터 | ⬜ 미실시 (AVD 시스템이미지 없음 — 온디바이스 최종 확인 권장) |

> 웹 구동 시 확인된 사항: 환율 API(open.er-api.com) 실제 연결됨(10,000동=570원 라이브). 스왑 시 방향·단가·권종칩 통화 자동 갱신 정상. 긴급전화/지도 연동은 실기기 전용이라 웹에선 미검증.
> 알려진 경미 이슈: 웹 캔버스에서 국기 이모지가 렌더링되지 않아 국가선택 칩 라벨이 짧게 보임(예: "베"). Android에선 정상 렌더 예상 → 온디바이스에서 확인 필요.

> 빌드 환경 메모: 시스템 JAVA_HOME이 JDK 1.7이라 Gradle 실패 → `flutter config --jdk-dir "C:\jdk-17.0.13+11"`로 JDK 17 지정해 해결.

## 6. 스토어 출시 체크리스트 (담당자 관점)

### Google Play
- [x] 권한 최소화 — `INTERNET` 하나만 사용 (위치·연락처 등 미요청)
- [x] 개인정보 미수집 설계 (계정 없음, 서버 없음, 로컬 저장만)
- [ ] **개인정보처리방침 URL** (Play 필수, 광고 없어도 권장) — 앱 내 링크 자리 준비됨
- [ ] **데이터 안전(Data safety) 양식** — "데이터 미수집" 신고
- [ ] targetSdk 최신 정책 레벨 확인 (Play 최소 요구 상향 추적)
- [ ] **업로드 키(keystore) 생성 + 서명 설정** (`key.properties`, `build.gradle`)
- [ ] 앱 아이콘 / 스플래시 / 스토어 스크린샷 / 설명
- [ ] 콘텐츠 등급 설문
- [x] 긴급/의료 정보 **면책 문구** (심사·책임 이슈 대비)

### App Store (추후)
- [ ] iOS `Info.plist` 쿼리 스킴(tel/maps) 및 사용목적 문구
- [ ] 광고 도입 시 **ATT(App Tracking Transparency)** 동의 필수
- [ ] 애플 심사: 정보성 앱 최소 기능성 요건 충족 여부

### 수익화 도입 시 (공통)
- [ ] AdMob 연동 시: 개인정보처리방침 필수 + **UMP 동의창**(GDPR) + iOS ATT
- [ ] 인앱결제(프리미엄): 광고 제거 / 오프라인 지도 / 프리미엄 큐레이션 (더보기 탭에 진입점 확보됨)

## 7. 다음 작업 (우선순위)

1. **실기기/에뮬레이터 구동 확인** (환율 실시간 호출·전화·지도 동작)
2. 앱 아이콘 · 스플래시 · 브랜드명 확정
3. 맛집/명소 **실데이터 큐레이션** (도시별, 관광객/현지인 태깅)
4. 개인정보처리방침 페이지 작성 + 앱 내 연결
5. 릴리즈 서명(keystore) 설정 → 내부 테스트 트랙 업로드
6. (선택) 오프라인 지도, 경비 가계부, 번역 회화 확장

## 8. 알려진 제약 / 메모

- 환율은 **참고용**(무료 API, 은행 고시환율과 차이 있음) — 앱 내 명시.
- 대만은 미수교국 → 대사관 대신 **대표부** 정보 사용.
- 맛집/명소는 현재 **샘플**. 실서비스 전 큐레이션 필수.
- 긴급번호·대사관 정보는 변동 가능 → 정기 점검 필요.
